{
  "name": "template-smart-contract-project",
  "version": "0.2.0",
  "files": [
    "src/**/*.sol"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.27.10",
    "dotenv": "^16.3.1",
    "husky": "^8.0.3",
    "lint-staged": "^14.0.1"
  },
  "lint-staged": {
    "{src,script,test}/**/*.sol": [
      "forge fmt"
    ]
  },
  "scripts": {
    "prepare": "husky"
  }
}
