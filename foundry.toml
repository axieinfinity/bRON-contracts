[profile.default]
src = "src"
out = "out"
script = "script"
libs = ["dependencies"]
ffi = true
script_execution_protection = false

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options

solc = '0.8.29'
evm_version = 'cancun'
use_literal_content = true
extra_output = ["devdoc", "userdoc", "storagelayout"]
fs_permissions = [{ access = "read-write", path = "./" }]
optimizer = true
optimizer_runs = 200
gas_limit = "18446744073709551615"

[fmt]
tab_width = 2
line_length = 120
bracket_spacing = true

[rpc_endpoints]
localhost = "http://localhost:8545"
ethereum = "https://eth.llamarpc.com"
ronin-mainnet = "https://api-archived.roninchain.com/rpc"
sepolia = "https://eth-sepolia.api.onfinality.io/public"
ronin-testnet = "https://saigon-testnet.roninchain.com/rpc"

[fuzz]
runs = 256

[invariant]
runs = 256

[dependencies]
"fdk" = { version = "0.3.6-gamma", url = "https://github.com/axieinfinity/foundry-deployment-kit/archive/refs/tags/v0.3.6-gamma.1.zip" }
"contract-libs" = { version = "0.1.7", url = "https://github.com/axieinfinity/contract-libs/archive/refs/tags/release-v0.1.7.zip" }
"solady" = { version = "0.1.19", url = "https://github.com/Vectorized/solady/archive/refs/tags/v0.1.19.zip" }
"openzeppelin-v4" = { version = "4.9.5", url = "https://github.com/OpenZeppelin/openzeppelin-contracts/archive/refs/tags/v4.9.5.zip" }
"creator-token-standard-v5" = { version = "5.0.0", url = "https://github.com/limitbreakinc/creator-token-standards/archive/refs/tags/v5.0.0.zip" }
"tstorish" = { version = "0.1.0", git = "git@github.com:ProjectOpenSea/tstorish.git", rev = "e0dd75e" }

[soldeer]
# whether soldeer manages remappings
remappings_generate = false

# whether soldeer re-generates all remappings when installing, updating or uninstalling deps
remappings_regenerate = false

# whether to suffix the remapping with the version: `name-a.b.c`
remappings_version = true

# a prefix to add to the remappings ("@" would give `@name`)
remappings_prefix = "@"

# where to store the remappings ("txt" for `remappings.txt` or "config" for `foundry.toml`)
# ignored when `soldeer.toml` is used as config (uses `remappings.txt`)
remappings_location = "txt"

# whether to install sub-dependencies or not. If true this wil install the dependencies of dependencies 1 level down.
recursive_deps = true
